## What changed

<!-- A sentence or two. What does this PR do? -->

## Why

<!-- The motivation, or the issue it closes (e.g. Closes #12). -->

## Type of PR

- [ ] Feature / fix / chore → merging into `dev`
- [ ] Release → promoting `dev` into `main`

## Checklist

- [ ] Branched off `dev` (not `main`)
- [ ] `npm run lint` and `npm run build` pass in `frontend/`
- [ ] Backend imports cleanly (`cd backend && python -c "from main import app"`)
- [ ] No databases (`*.db`), logs, `venv/`, or Cricsheet archives committed

## Notes for the reviewer

<!-- Anything worth flagging: trade-offs, follow-ups, things you deliberately left out. -->
